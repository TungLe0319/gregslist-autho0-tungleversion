let dbContext;_1b4‍.x([["carsService",()=>carsService]]);_1b4‍.w("../db/DbContext.js",[["dbContext",["dbContext"],function(v){dbContext=v}]]);

class CarsService {
  async createCar(formData) {
    const car = await dbContext.Cars.create(formData);

    return car
  }

  async getCars() {
    const cars = await dbContext.Cars.find();
    return cars;
  }
}

       const carsService = new CarsService();
